const add = (x, y) => {
  return x + y;
};

const subtract = (x, y) => {
  return x - y;
};

const multiply = (x, y) => {
  return x * y;
};

const divide = (x, y) => {
  return x / y;
};

const pi = 3.14;

module.exports = {
  add,
  subtract,
  multiply,
  divide,
  pi,
};
